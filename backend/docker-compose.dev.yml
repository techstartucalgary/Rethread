version: "3"
services:
  fashion-node-app:
    build:
      context: .
      args:
        - NODE_ENV=development
    ports:
      - 8080:8080
    volumes:
      - .:/app
      - /app/node_modules
    env_file: .env
    command: npm run dev
